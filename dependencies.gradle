def kotlinVersion = "1.2.30"

ext {
    android = [
            applicationId : "com.oc.onecup",
            compileSdkVersion : 28,
            minSdkVersion : 16,
            targetSdkVersion : 28,
            versionCode : 1,
            versionName : "1.0"
    ]
}