def kotlinVersion = "1.2.30"
def supportVersion = "28.0.0"

ext {
    android = [
            applicationId    : "com.oc.onecup",
            compileSdkVersion: 28,
            minSdkVersion    : 16,
            targetSdkVersion : 28,
            versionCode      : 1,
            versionName      : "1.0"
    ]

    dependencies = [
            "appcompat-v7": "com.android.support:appcompat-v7:${supportVersion}"

    ]

}